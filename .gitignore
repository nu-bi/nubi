# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

node_modules
__pycache__/
*.pyc

# Environment variables
.env
.env.*
backend/.env

# Secret/credential files
backend/storage/secret-files/
key.json
keyfile.json
keyfile*.json
*.scoped.json

# Firebase
.firebase/

# Internal billing/pricing scenarios — not part of the product, not for git
billing-model/

legacy/
# Generated DuckDB seed data files (built by backend/seed_data_duckdb.py).
# NOTE: only the generated *.duckdb files are ignored — the declarative demo
# fixtures in backend/seed_data/demo/*.json ARE tracked (source of truth).
*.duckdb

# Generated local demo parquet files (written on demand by
# backend/app/demo_bundle.export_demo_parquet_local).
backend/seed_data/parquet/

# Playwright test artifacts (generated; the 500KB+ report HTML otherwise breaks
# Vite's dependency scan — also excluded in vite.config.js optimizeDeps.entries).
playwright-report/
test-results/
