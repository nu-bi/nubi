/**
 * TierCard.jsx — EE re-export of the core TierCard component.
 *
 * The core TierCard lives at src/components/pricing/TierCards.jsx.
 * This module is kept so existing EE imports of './TierCard.jsx' continue
 * to resolve without changes.
 */

export { TierCard as default } from '../../components/pricing/TierCards.jsx'
