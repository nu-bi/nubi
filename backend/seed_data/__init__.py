"""Bundled demo seed data: declarative fixtures + deterministic generators.

- ``demo/``       : queries.json / boards.json — the declarative demo fixtures.
- ``generators/`` : deterministic dataset generators (one module per dataset).
- ``parquet/``    : local parquet export target (generated, gitignored).
"""
